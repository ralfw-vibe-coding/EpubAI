- [x] im login dialog soll der cursor sofort im eingabefeld für die email stehen
- [x] im OTP dialog soll der cursor sofort im eingabefeld für das OTP stehen

---
Erledigt: 2026-07-15 11:38, direkt umgesetzt (keine Agents nötig, triviale Änderung):
- Beide Eingabefelder im Login-Flow (`frontend/src/routes/login/+page.svelte`) haben jetzt `autofocus`; Svelte's a11y-Warnung dazu ist bewusst per `svelte-ignore a11y_autofocus` unterdrückt (Login-Formular ist genau der Fall, für den `autofocus` gedacht ist).

Verifiziert: `npm run check` (0 Fehler/Warnungen), `npm test` (87/87), `npm run coverage` (unverändert, Portal-Seiten sind von der Deckungspflicht ausgenommen), sowie live im Browser: E-Mail-Feld hat beim Laden den Fokus, nach "Code anfordern" springt der Fokus automatisch ins Code-Feld.
