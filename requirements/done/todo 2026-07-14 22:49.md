- [x] Wenn ich ein Tag eingebe, will ich die ganze Liste schon vorhandener Tags sehen. Sie soll mit jedem eingetippten Buchstaben weiter eingeschränkt werden. Ich kann ein neuer Tag eingeben oder ein bestehendes auswählen.
- [x] In der Listenansicht pro Buch auch die vergebenen Tags anzeigen wie in der Cover-Ansicht.
- [x] Größe der Schrift für "Schließen" genauso wie für "zurück" und "weiter".
- [x] Fortschritt und Seitenzahlen sollen nicht mehr über dem Text stehen im Reader. Dort gehört der Buchtitel hin.

---
Erledigt: 2026-07-14 22:49, direkt umgesetzt (keine Agents nötig, kleine gut verstandene Änderungen):
- Tag-Vorschläge zeigen jetzt bei Fokus die volle Liste noch nicht zugewiesener Tags, tippen schränkt ein (kein Cap mehr, Container scrollt bei langer Liste).
- Listenansicht im Katalog zeigt jetzt Tags wie die Cover-Ansicht.
- "Schließen" im Reader-Header ist jetzt genauso groß (14px) wie "Zurück"/"Weiter".
- Reader-Header zeigt jetzt den Buchtitel (direkt aus den EPUB-Metadaten via epub.js, kein Netzwerkaufruf, funktioniert offline) statt Fortschritt/Seitenzahl; Fortschritt bleibt in der Fußleiste.

Verifiziert: `npm run check` (0 Fehler), `npm test` (62/62), `npm run build` sauber, sowie live im Browser (Tag-Vorschlagsliste bei Fokus + Einschränken beim Tippen, Tags in Listenansicht, Schriftgrößen-Vergleich per computed style, Buchtitel im Reader-Header).
