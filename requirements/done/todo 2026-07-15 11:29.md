- [x] In der Cover und Listenübersicht soll mit einem Symbol angezeigt werden, dass ein Buch gerade ausgeliehen ist (auf dem Gerät). Kann das zb ein weißer Haken in einem roten Kreis (das Rot, das auch sonst schon in der App benutzt wird) auf dem Buchcover rechts unten sein?
- [x] Bei den Settings für ein Buch im Reader sehe ich noch keinen Unterschied, wenn ich die Ränder einstelle.
- [x] Baue aus dem media asset logo ein Favicon für die App.

---
Erledigt: 2026-07-15 11:29, direkt umgesetzt (keine Agents nötig, kleine gut verstandene Änderungen):
- Katalog (Cover- und Listenansicht) zeigt jetzt einen weißen Haken in einem roten Kreis unten rechts auf dem Cover, wenn das Buch auf diesem Gerät ausgeliehen ist. Dafür liefert `loadCatalog` jetzt `BookDetail[]` statt `CatalogBook[]` (neue Domain-Methode `detailsFor` enrichert den ganzen Katalog in einem Rutsch mit Loan-Status und Fortschritt, statt wie vorher nur Fortschritt einzeln zu mappen).
- Der Ränder-Regler im Reader wirkte bisher nicht: epub.js setzt beim Paginieren selbst ein `!important`-Inline-Padding auf `<body>`, das ein per Stylesheet injiziertes Padding (auch mit `!important`) immer aussticht. Die Ränder werden jetzt stattdessen als Padding auf den eigenen Reader-Container gelegt (epub.js misst dadurch selbst eine schmalere Fläche), inklusive explizitem `rendition.resize()` bei nachträglicher Änderung während des Lesens.
- Favicon aus dem Logo (`media assets/epubai logo.png`) generiert (16/32/192/512px + Apple-Touch-Icon), altes Platzhalter-SVG entfernt, in app.html verlinkt.

Nebenbei behoben: ein Absturz ("Cannot read properties of undefined (reading 'resize')"), der durch einen zu früh (vor epub.js' eigenem async Setup) aufgerufenen `resize()` beim Öffnen des Readers entstand - der Aufruf war beim ersten Rendern ohnehin unnötig, da das Container-Padding schon vor dem Rendern im DOM steht.

Verifiziert: `npm run check` (0 Fehler), `npm test` (87/87), `npm run coverage` (99.3%/92.4%), `npm run build` sauber, sowie live im Browser vom Nutzer bestätigt (Ränder wirken sichtbar, Haken erscheint auf dem Cover ausgeliehener Bücher, Favicon sichtbar).
