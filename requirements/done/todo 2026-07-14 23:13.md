- [x] Nach dem Hochladen kann ich sofort Metadaten bearbeiten. Aber ich kann keine Tags vergeben. Das soll anders werden. Tags sollen auch dort so erfasst werden können wie bei der Bearbeitung von schon geladenen Büchern.
- [x] Ich sehe im Buch im Reader als Titel nur "Alice" über dem Text, während ich in den Metadaten den Titel auf "Alice - A Modernn Novel" geändert habe.
- [x] In der Listenansicht das Cover größer darstellen. Es kann über Titel/Autor/Tags hinausragen und fast die ganze Höhe des Listeneintrags ausfüllen. Nur einen schmalen Rand lassen.

---
Erledigt: 2026-07-14 23:13, direkt umgesetzt (keine Agents nötig, kleine gut verstandene Änderungen):
- Der Upload-Bestätigungsdialog hat jetzt denselben Tag-Editor (Chips, Eingabefeld, Vorschlagsliste) wie die Bearbeitung bestehender Bücher; Tags werden direkt beim Anlegen mitgespeichert (Backend: `createBook` akzeptiert jetzt optional `tags`).
- Der Reader zeigt den Titel nicht mehr aus den (unveränderlichen) EPUB-Metadaten, sondern aus dem lokal gecachten Titel des Loans (offline-first: kein Netzwerkaufruf nötig). Der Loan bekommt seinen Titel beim Ausleihen und wird bei jeder Titeländerung eines gerade ausgeliehenen Buchs automatisch nachgezogen (`renameLoanIfPresent`). Bestehende (alte) Loans ohne gecachten Titel fallen weiterhin auf die EPUB-Metadaten zurück.
- In der Listenansicht ist das Cover jetzt deutlich größer (aspect-[2/3] h-28 statt h-16 w-12), der Listeneintrag hat weniger vertikales Innenpolster.

Verifiziert: `npm run check` (0 Fehler), `npm test` (69/69), `npm run coverage` (99.25% Stmts, 92.07% Branches, 98.27% Funcs - alle Schwellwerte weit übertroffen), `npm run build` sauber, sowie live im Browser: Upload mit Tag-Vergabe, Reader-Titel bleibt beim Ausleihen und nach nachträglicher Titeländerung korrekt synchron (kein Rückfall auf den alten EPUB-Titel "Alice"), größere Cover in der Listenansicht.
